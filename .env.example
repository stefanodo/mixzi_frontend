VITE_MIXZI_API_BASE=http://localhost:8080
VITE_NAME=dev
VITE_MOCKED_VALUES=false
VITE_DISABLE_SSE=true

# URL base de Supabase Auth/Kong (login/signup van directo del frontend a Kong, no via el backend).
# Reservada en Docker (Dockerfile + docker-compose.yml) para cuando se implemente el consumo en src/config/envVariables.ts.
VITE_MIXZI_AUTH_BASE=https://mixzi-auth-dev.granile.dev
